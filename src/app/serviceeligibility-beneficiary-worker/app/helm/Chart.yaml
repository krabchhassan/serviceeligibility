apiVersion: v2
name: serviceeligibility-beneficiary-worker
description: Helm chart for serviceeligibility-beneficiary-worker in serviceeligibility service.
type: application
appVersion: "${imageTag}"
version: 0.0.0
dependencies:
  - name: helm-chart-beyond-common
    repository: "oci://registry-dev.beyond.cegedim.cloud/devops-infra"
    version: v16.0.7
