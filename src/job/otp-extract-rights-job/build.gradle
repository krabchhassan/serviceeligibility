plugins {
    id("beyond.gradle.plugins.python-app")
}

pythonAppExtension {
    unitTestsDirectoryPath = "unit"
    setActivateLint(true)
}

dependencies {
    lib(project(":lib-otp-common"))
}

tasks.named("dockerPrepare") {
    from("entrypoint.sh")
}
